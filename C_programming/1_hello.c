#include <stdio.h> // header file for std. in/out like printf

int main() //entry point of a program
{
    printf("hello!\n");

    return 0;
}

// const keyword : value can't be changed while program execution.
// #define : using preprocessor directive
// The difference between const and #define is that the former uses memory for storage and the latter does not.